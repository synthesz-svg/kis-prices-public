services:
  - type: web
    name: kis-price-relay
    runtime: python
    plan: free
    buildCommand: pip install -r relay_requirements.txt
    startCommand: gunicorn relay_app:app
    autoDeployTrigger: off
